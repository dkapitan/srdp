# Everything stage's Flux reconciles (path: deploy/clusters/stage). Each child keeps
# its own namespace. Add future per-env platform pieces here.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../platform/clusters/stage
  - ../../srdp/clusters/stage
