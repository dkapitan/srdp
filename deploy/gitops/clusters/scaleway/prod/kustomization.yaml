# Everything prod's Flux reconciles (path: deploy/gitops/clusters/scaleway/prod).
# Shared platform pieces come from platform/base. Scaleway secret-store /
# secret-mapping bindings will be re-added here once tested end to end.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../platform/base
